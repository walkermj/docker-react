sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t walkermj/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true walkermj/docker-react npm run test


